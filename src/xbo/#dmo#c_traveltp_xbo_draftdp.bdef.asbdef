projection;
strict ( 2 );
extensible;
use side effects;
use draft as dependent;

define behavior for /DMO/C_TRAVELTP_XBO_DRAFTDP alias Travel
extensible
use etag
{
  use create;
  use update;
  use delete;

  use association _Agency { with draft; }
}