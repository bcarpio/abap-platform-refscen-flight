projection;
strict ( 2 );
extensible;
use side effects;
use draft;

define behavior for /DMO/C_AGENCYTP_XBO alias Agency
extensible
use etag
{
  use create;
  use update;
  use delete;

  use action Activate;
  use action Discard;
  use action Edit;
  use action Resume;
  use action Prepare;

  use association _Travel {
    create;
    with dependent draft;
  }
}